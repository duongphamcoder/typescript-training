enum TypeNotify {
    DELETE = 'Deleted',
    CREATE = 'Create',
    UPDATE = 'Update',
}

enum NotifyMessage {
    DELETE = 'Deleted to-do successful',
    CREATE = 'Create to-do successful',
    UPDATE = 'Update to-do successful',
}

export { TypeNotify, NotifyMessage };

